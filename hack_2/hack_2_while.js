/**
 * mediante el while for agregar los números 1,2,3,4,5
 * dentro del array result
 *
 *
 * output => [1,2,3,4,5]
 */
let result = [];
let counter = 0;

while (counter < 5) {
  counter++;
  result.push(counter);
}

//export result
module.exports = result;
